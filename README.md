# ModuStackAdmin

ModuStack — Starter kit backoffice: roles, permisos, auditoría y 2FA

**Repositorio:** https://github.com/rulos26/ModuStackAdmin


## Docker

```bash
docker compose up --build
```

App: http://localhost:8085

En Laravel, el .env debe usar `DB_HOST=mysql` y `DB_PORT=3306` cuando corre en Docker.
